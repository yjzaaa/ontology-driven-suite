# `.autoclaw/vector/db.sqlite` — Vector store

> Auto-generated by AutoClaw (contract v1).

**Written by:** `/index-code` (code chunks) and `/learn` (learning embeddings)

**Contains:** Embeddings of code + learnings for semantic retrieval (`/retrieve`, `/search`).

**What it is NOT:** NOT a knowledge graph — it holds vectors, not nodes/edges.

See `.autoclaw/AGENT-ORIENTATION.md` for the full command + store contract.
