# `.autoclaw/kg/kg.db` — Knowledge Graph

> Auto-generated by AutoClaw (contract v1).

**Written by:** `/learn` (coordination outcomes) and the orchestrator

**Contains:** Multi-agent COORDINATION facts as nodes/edges: consensus verdicts, review findings, and related decisions.

**What it is NOT:** NOT a code-symbol / dependency / cross-reference graph, and `/index-code` does NOT write here. For code relationships use vector retrieval (`/retrieve`).

See `.autoclaw/AGENT-ORIENTATION.md` for the full command + store contract.
