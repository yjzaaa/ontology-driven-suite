# 手工验收题单（V2 引擎泛化后）

16 题全部实测验证（本轮，write_mode=local_json，LLM=gpt-5）。
在 `http://localhost:8000` 的对话界面逐题输入，对照"预期结果"核对。

## A. 同一行、不同视图（V2 类型化投影核心体验）

| # | 输入 | 预期 |
|---|---|---|
| 1 | `查文档 9305733` | **全景视图**（pr_approval_doc，5 列：文档类型/状态/备注/更新时间/创建时间） |
| 2 | `查厂商 9305733` | **厂商视图**（vendor_type，6 列：状态/厂商编码/厂商名称/厂商类型/内容状态/最后修改），1 行 |
| 3 | 对比 1/2 | 同一个 id=5138，两套投影——引擎 type_filter+content_fields 能力，无业务代码 |

## B. 检索与同义词（关键词来自本体 YAML，LLM 与目录路由双兼容）

| # | 输入 | 预期 |
|---|---|---|
| 4 | `供应商 9305733` | 厂商视图 1 行（"供应商"= YAML keywords 同义词） |
| 5 | `查物料 芝士` | 商品视图 4 行，19 列（含"IT 资产类型"） |
| 6 | `查商品 Teabreak` | 商品视图 50 行（行数上限生效，实际匹配更多） |
| 7 | `查商品 芝士烤肠` | 商品视图 1 行 |
| 8 | `查厂商 SIEMENS` | 厂商视图 50 行 |
| 9 | `查预算 FY23_Ext testing` | 预算视图 1 行，预算三件套（预算金额/已用金额/剩余预算） |
| 10 | `查参考号 AT1.11` | 预算视图 1 行（"参考号"同义词命中） |

## C. 写路径 HITL（提案 → 批准 → 读写自洽）

| # | 输入 | 预期 |
|---|---|---|
| 11 | `请把 9305733 停用` | HIGH 提案卡：`启停审批主数据文档：PR 审批主数据文档（id=5138） \| 状态 → Inactive`；批准后 `LOCAL_SANDBOX_APPLIED`（沙箱诚实码） |
| 12 | `9305733 这条文档现在什么状态？` | 状态=Inactive（批准结果对查询可见） |
| 13 | `帮我把 9305733 重新启用了` | 口语化解析为启用，批准后恢复 Active |

## D. 语义护栏（必须拒绝，绝不猜测近似动作）

| # | 输入 | 预期 |
|---|---|---|
| 14 | `把芝士的单价改成 6000` | **未识别意图**——商品编辑行为未建模，M3 规则不产生幻影能力 |
| 15 | `帮我把 9305733 的厂商名称改成 Siemens Healthineers` | **未识别意图**——厂商编辑行为未建模 |
| 16 | `把 9305733 删掉` | **未识别意图**（删除不在动作目录） |

## 附加观察点

- **路由行**：每次回答顶部的路由说明（LangGraph 编排 · LLM / 未识别意图）。
- **审计**：`gateway/data/audit.jsonl` 记录每次治理查询与提案事件；
  规则拒绝会出现 `proposal_rejected_by_rule`（当前聊天写路径不触字段级规则，
  该事件主要在 V2.1 编辑行为上线后出现）。
- **幂等**：C 组结束状态=Active，可重复运行。
- 题单中所有检索值均为本地库真实值（Teabreak/芝士烤肠/AT1.11/FY23_Ext testing）。
