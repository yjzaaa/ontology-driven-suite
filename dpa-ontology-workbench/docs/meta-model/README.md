# Reverse-engineering meta-model

本目录遵循 `codebase-reverse` 的产物契约，保存 DPA 源码资产、功能、接口、对象、数据库、权限、配置、调用链、覆盖率和一致性报告。

这里的报告用于解释和审核，不作为 Runtime Registry 的输入。机器可读证据进入 `evidence/snapshots/<revision>/`，正式运行时模型进入 `models/<domain>/`。
