# Ontology Gateway

Gateway 计划提供 Ontology Registry、语义 Tool、Policy、Action Proposal、HITL、MI Resolver、DPA HTTP Executor、Database MCP Adapter 和审计。

Agent 可见层不能读取 MI 技术目标或 DPA 凭据；只有内部 Execution 模块可以解析已发布 MI。
